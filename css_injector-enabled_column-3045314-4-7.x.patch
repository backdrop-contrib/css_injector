diff --git a/css_injector.admin.inc b/css_injector.admin.inc
index d788bf9..637a474 100644
--- a/css_injector.admin.inc
+++ b/css_injector.admin.inc
@@ -15,33 +15,36 @@ function css_injector_admin_form($form, &$form_state) {
   $form = array();
   $form['#tree'] = TRUE;
 
-  // Adding css stylesheet for icons.
-  $form['#attached']['css'] = array(
-    drupal_get_path('module', 'css_injector') . '/css_injector.css',
-  );
-
   foreach ($rules as $rule) {
     $form['rules'][$rule['crid']]['#rule'] = $rule;
-    $form['rules'][$rule['crid']]['edit'] = array(
-      '#type' => 'submit',
-      '#value' => t('Edit rule'),
-      '#name' => 'edit' . $rule['crid'],
-      '#submit' => array('css_injector_admin_edit_button'),
-      '#attributes' => array('class' => array('css-inject-edit')),
-      '#crid' => $rule['crid'],
-    );
-    $form['rules'][$rule['crid']]['delete'] = array(
-      '#type' => 'submit',
-      '#value' => t('Delete rule'),
-      '#name' => 'delete' . $rule['crid'],
-      '#submit' => array('css_injector_admin_delete_button'),
-      '#attributes' => array('class' => array('css-inject-delete')),
-      '#crid' => $rule['crid'],
+    $form['rules'][$rule['crid']]['enabled'] = array(
+      '#type' => 'checkbox',
+      '#default_value' => $rule['enabled'],
     );
   }
+  $form['submit'] = array(
+    '#type' => 'submit',
+    '#value' => t('Save configuration'),
+  );
   return $form;
 }
 
+/**
+ * Submit button callback for the admin overview form.
+ */
+function css_injector_admin_form_submit($form, &$form_state) {
+  $rules = $form_state['values']['rules'];
+  foreach ($rules as $crid => $rule) {
+    $num_updated = db_update('css_injector_rule')
+      ->fields(array(
+        'enabled' => $rule['enabled'],
+      ))
+    ->condition('crid', $crid)
+    ->execute();
+  }
+  drupal_set_message(t('Configuration saved.'));
+}
+
 /**
  * Edit button callback for the CSS rule listing form.
  */
@@ -66,7 +69,7 @@ function css_injector_admin_delete_button($form, &$form_state) {
  */
 function theme_css_injector_admin_form($variables) {
   $form = $variables['form'];
-  $headers = array(t('Title'), t('Location'), t('Actions'));
+  $headers = array(t('Title'), t('Location'), t('Enabled'), t('Actions'));
   $rows = array();
   if (!empty($form['rules'])) {
     foreach (element_children($form['rules']) as $crid) {
@@ -74,28 +77,12 @@ function theme_css_injector_admin_form($variables) {
       $rule = $form['rules'][$crid]['#rule'];
       $row[] = check_plain($rule['title']);
       $row[] = _css_injector_rule_uri($rule['crid']);
-      $row[] = drupal_render($form['rules'][$crid]);
+      $row[] = array('data' => drupal_render($form['rules'][$crid]['enabled']), 'align' => 'center');
+      $row[] = l(t('edit'), 'admin/config/development/css-injector/edit/' . $crid) . ' &nbsp; ' . l(t('delete'), 'admin/config/development/css-injector/delete/' . $crid);
       $rows[] = $row;
     }
   }
-
-  $link = l(t('Create a new rule'), 'admin/config/development/css-injector/add');
-  $row = array();
-  if (empty($rows)) {
-    $row[] = array(
-      'data' => t('No CSS injection rules have been set up yet. !url.', array('!url' => $link)),
-      'colspan' => 3,
-    );
-  }
-  else {
-    $row[] = array(
-      'data' => t('!url.', array('!url' => $link)),
-      'colspan' => 3,
-    );
-  }
-  $rows[] = $row;
-
-  $output = theme('table', array('header' => $headers, 'rows' => $rows));
+  $output = theme('table', array('header' => $headers, 'rows' => $rows, 'empty' => t('No CSS injection rules have been set up yet.')));
   $output .= drupal_render_children($form);
   return $output;
 }
diff --git a/css_injector.css b/css_injector.css
deleted file mode 100644
index d5a8dc0..0000000
--- a/css_injector.css
+++ /dev/null
@@ -1,12 +0,0 @@
-form input.css-inject-edit,
-form input.css-inject-delete {
-  padding-left: 35px;
- background-position: 12px center;
- background-repeat: no-repeat;
-}
-form input.css-inject-edit {
- background-image: url('text-editor.png');
-}
-form input.css-inject-delete {
-  background-image: url('edit-delete.png');
-}
\ No newline at end of file
diff --git a/css_injector.module b/css_injector.module
index 03d7f5d..04d0da2 100644
--- a/css_injector.module
+++ b/css_injector.module
@@ -134,7 +134,7 @@ function css_injector_menu() {
       'page arguments' => array('css_injector_edit'),
       'access arguments' => array('administer css injection'),
       'file' => 'css_injector.admin.inc',
-      'type' => MENU_CALLBACK,
+      'type' => MENU_LOCAL_ACTION,
     ),
     'admin/config/development/css-injector/delete' => array(
       'title' => 'Delete CSS injector rule',
